export {
  countryLabel,
  runLocalDetector,
  runNetworkProbe,
  summarizeNetworkGeo,
} from "./detector";

export type {
  DetectionReport,
  NetworkGeoSummary,
  NetworkProbeResult,
  RiskLevel,
  Signal,
} from "./detector";
